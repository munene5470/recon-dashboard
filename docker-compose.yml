services:
  recon-dashboard:
    build: .
    ports:
      - "4000:4000"
    environment:
      PORT: 4000
      NODE_ENV: production
    volumes:
      - ./data:/app/data
      - ./recon-output:/app/recon-output
    restart: unless-stopped
