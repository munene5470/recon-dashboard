services:
  recon-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
    volumes:
      - ./data:/app/data
      - ./recon-output:/app/recon-output
    restart: unless-stopped
